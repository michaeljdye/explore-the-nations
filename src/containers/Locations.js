import React, { Component } from 'react';

export default class Locations extends Component {
  render() {
    return (
      <div>
        <p>{this.props.location} Works!</p>
      </div>
    );
  }
}
